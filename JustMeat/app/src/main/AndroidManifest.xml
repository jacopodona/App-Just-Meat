<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.justmeat">
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".homepage.HomepageActivity" android:theme="@style/AppTheme" android:exported="true"></activity>
        <activity android:name=".welcome.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".marketview.MarketViewActivity"/>
        <activity android:name=".carrello.CarrelloActivity"/>
        <activity android:name=".checkout.CheckoutActivity"/>
        <activity android:name=".signup.SignupActivity" android:exported="true"></activity>
        <activity android:name=".login.LoginActivity" android:exported="true"></activity>
    </application>
</manifest>
